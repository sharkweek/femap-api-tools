Sub Main
    Dim App As femap.model
    Set App = feFemap()

    App.feAppMessage(FCM_COMMAND, "Output from Multiple Load Sets")
    App.feAppMessage(FCM_NORMAL, "Batch create output set data from multiple load sets.")

    Dim rc As Long

    

End Sub
